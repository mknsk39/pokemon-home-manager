rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // ユーザープロフィール: 本人のみ読み書き可能
    match /users/{userId} {
      allow read, write: if request.auth != null && request.auth.uid == userId;
    }

    // ポケモン所持状況: 本人のみ読み書き可能
    match /userOwnership/{userId} {
      allow read, write: if request.auth != null && request.auth.uid == userId;
    }
  }
}
